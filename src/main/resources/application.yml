server:
  port: 9999

token:
  name: lxToken

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/boarddb?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
